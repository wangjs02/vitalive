# Data belongs in the shared external data root, not inside prod.
# Keep this rule to prevent accidental dataset commits if prod/data is created.
data/*

# Local checkpoints, weights, and model outputs
model/*
!model/.gitkeep

# Python/runtime noise
__pycache__/
*.py[cod]
.DS_Store
.ipynb_checkpoints/
